let africastalking;_94b‍.w("africastalking",[["default",["africastalking"],function(v){africastalking=v}]]);

const mysql = require("mysql");

const conn = mysql.createConnection({
  host: "localhost",
  user: "root",
  password: "Cyberman@2999",
  database: "users",
  // port: "3302",
});

conn.connect(function (err) {
  if ( err ) throw err;
  conn.query("SELECT phone_number FROM user", function (err, results, fields) {
    if (err) throw err;
    // console.log( results );
    const phone = results.map( (result) => {
      return result.phone_number;
    } ) 
    phone_numbers = phone;
    _94b‍.g.console.log(phone_numbers);
    
      
      const client = africastalking({
        apiKey:
          "0667ea64781b9fc514a0f44ca00611cd136be59c7783ec2d7d05ac6cf1ba4df0", // use your sandbox app API key for development in the test environment
        username: "sandbox",
      });

      client.SMS.send({
        to: ["+265996842416"],
        message: "Hey there, i sends her regards",
        from: "ULIMI-WATHU",
      })
        .then(() => console.log("Message sent successifuly"))
        .catch((err) => _94b‍.g.console.log(err));
      // console.log(`${result.phone_number}`);
    ;
  });
});


